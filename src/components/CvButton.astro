---
import LinkButton from './LinkButton.astro';

export interface Props {
  text: string;
  href: string;
}
const { text, href } = Astro.props;
---
<LinkButton href={href} text={text} iconName="mdi:file-document-outline" />